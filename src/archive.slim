
/ TODO: Update this when we have post groupings
- for post in site['posts', default: []]
  time datetime='{{post.date | date: "%Y-%m-%d"}}' {{post.date | date: "%d/%m/%Y"}}
  a href="{{post.url}}" {{ post.title }}
