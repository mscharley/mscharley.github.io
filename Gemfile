require 'rbconfig'
source 'https://rubygems.org'

gem 'jekyll'
gem 'wdm', '~> 0.1.0' if RbConfig::CONFIG['target_os'] =~ /mswin|mingw/i
gem 'pygments.rb', '0.5.0' #Windows doesn't support over this version properly

gem 'sass'
gem 'compass'
gem 'jekyll-compass'
gem 'foundation'

gem 's3_website'
gem 'rake'
